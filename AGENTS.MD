When writing typescript code, export only if needed.
Prefer descriptive variable names over ambiguous abbreviations used solely to make code shorter.
Keep code comments minimal; only explain non-standard patterns
Prefer surgical changes. When work spans multiple logical steps, create small, dependency-ordered commits so each commit can be reviewed sequentially; keep unrelated changes separate.

User has ADHD, prioritize key words and bullet points rather than natural conversation.
