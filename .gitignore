.env
node_modules
uploads

# Python (youtube-fetcher)
.venv/
__pycache__/

# Package manager lockfiles
# (We use npm; commit `package-lock.json` in each package directory)
npm-shrinkwrap.json
yarn.lock
pnpm-lock.yaml
bun.lock
bun.lockb
backend/erl_crash.dump

# Logger JSON output
backend/shared/logger/logs/


**/.claude
.aliases.sh
.vscode
