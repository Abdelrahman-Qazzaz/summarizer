{
  "name": "summarizer",
  "private": true,
  "packageManager": "npm@11.14.1",
  "scripts": {
    "browser:auth": "npm run --silent browser:auth --prefix backend",
    "dev": "concurrently -k -n backend,client,fetcher -c blue,green,yellow \"npm run dev --prefix backend\" \"npm run dev --prefix client\" \"npm run dev:fetcher\"",
    "stack": "npm run --silent doppler -- docker compose up --build",
    "doppler": "set -a; [ -f backend/.env ] && . ./backend/.env; set +a; exec mise exec -- doppler run --",
    "dev:fetcher": "until curl -sf http://localhost:3001/contract > /dev/null; do sleep 1; done && cd youtube-fetcher && { set -a; [ -f .env ] && . ./.env; set +a; exec mise exec -- doppler run -- .venv/bin/python app/app.py; }",
    "prepare": "simple-git-hooks"
  },
  "lint-staged": {
    "{backend,client}/**/*.{css,html,js,json,md,ts,tsx,yaml,yml}": "prettier --write",
    "youtube-fetcher/**/*.py": "youtube-fetcher/.venv/bin/ruff format"
  },
  "simple-git-hooks": {
    "pre-commit": "npx lint-staged"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "knip": "^6.29.0",
    "lint-staged": "^17.3.0",
    "mise": "2026.8.6",
    "prettier": "^3.9.6",
    "simple-git-hooks": "^2.13.1"
  }
}
