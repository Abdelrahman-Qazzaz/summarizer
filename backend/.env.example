# Base (API + worker)
DATABASE_URL=
MQ_URL=
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
OPENROUTER_API_KEY=
NODE_ENV=development

# API only (use localhost everywhere in dev — not 127.0.0.1 — so cookies work with Socket.IO)
SESSION_SECRET=
WORKOS_API_KEY=
WORKOS_CLIENT_ID=
CLIENT_URL=http://localhost:5173
PORT=3001
WS_PORT=4000

# Worker only
TRANSCRIBE_SUMMARIZE_SERVICE_PORT=3002
